version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No dependencies to install for static site"
    build:
      commands:
        - echo "No build process needed - serving static files"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
